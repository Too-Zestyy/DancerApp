services:
  dancer-back-end:
    # image: dancer-app-backend
    volumes:
      - type: bind
        source: ./app
        target: /app
    build: 
      context: .
      dockerfile: ./dancer-dev.dockerfile
    ports:
        - "5000:5000"

# volumes:
#   src:
#     driver: local
#     driver_opts:
#       type: none
#       o: bind
#       device: ./app